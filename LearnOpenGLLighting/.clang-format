# .clang-format

BasedOnStyle: LLVM    # Set the style to LLVM

IndentWidth: 4        # Set the indentation width (LLVM style uses 4 spaces)
ColumnLimit: 80       # Maximum line length before wrapping
UseTab: Never         # Use spaces for indentation (no tabs)
AllowShortFunctionsOnASingleLine: false  # Disallow short functions on one line

# Additional customization for LLVM style
BreakBeforeBraces: Allman  # Use the Allman style for braces (brace on a new line)
SpaceBeforeParens: ControlStatements  # Space before parentheses for control statements (e.g., `if (x)`)

# You can add more customizations as needed
